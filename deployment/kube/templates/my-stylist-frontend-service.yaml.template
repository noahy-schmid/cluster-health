apiVersion: v1
kind: Service
metadata:
  name: my-stylist-frontend
  namespace: ${NAMESPACE}
  labels:
    app: my-stylist-frontend
spec:
  selector:
    app: my-stylist-frontend
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
