// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// source: deinsalon/v1/appointment_events.proto
// Regenerate with: pnpm generate:types
/* eslint-disable */

export const protobufPackage = "deinsalon.v1";

/** Emitted by the appointment service when a new appointment is successfully booked. */
export interface AppointmentBooked {
  appointmentId: string;
  salonId: string;
  stylistId: string;
  serviceId: string;
  /** ISO 8601 datetime (UTC) */
  startsAt: string;
  /** ISO 8601 datetime (UTC) */
  endsAt: string;
  /** ID of the customer who made the booking */
  customerId: string;
}

/** Emitted when an existing appointment is cancelled. */
export interface AppointmentCancelled {
  appointmentId: string;
  salonId: string;
  /** Who initiated the cancellation: "customer", "stylist", or "system" */
  cancelledBy: string;
  reason: string;
}

/** Emitted when a stylist's availability for a given day is updated. */
export interface StylistAvailabilityUpdated {
  salonId: string;
  stylistId: string;
  /** ISO 8601 date (YYYY-MM-DD) */
  date: string;
  availableSlots: TimeSlot[];
}

/** A discrete availability window within a day. Times are in HH:MM (UTC). */
export interface TimeSlot {
  startTime: string;
  endTime: string;
}
